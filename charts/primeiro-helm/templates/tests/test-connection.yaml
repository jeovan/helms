apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "primeiro-helm.fullname" . }}-test-connection"
  labels:
    {{- include "primeiro-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "primeiro-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
